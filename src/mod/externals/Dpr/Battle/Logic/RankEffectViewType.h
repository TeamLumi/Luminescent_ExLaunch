#pragma once

#include "externals/il2cpp-api.h"

namespace Dpr::Battle::Logic {
    enum class RankEffectViewType : uint8_t {
        ENABLE = 0,
        ENABLE_FORCE = 1,
        DISABLE = 2,
    };
}